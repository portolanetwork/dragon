turnstile {
  grpc {
    host = "0.0.0.0"
    port = 8080
  }

  cluster {
    num-shards = 24
  }

  mcp-stdio {
    enabled = false
    name = "Dragon MCP Stdio Server"
    version = "1.0.0"
  }

  mcp-streaming {
    enabled = true
    name = "Dragon MCP Streaming Server"
    version = "1.0.0"
    host = "0.0.0.0"
    port = 8082

    # HTTPS/TLS Configuration (optional)
    # Uncomment and configure to enable HTTPS
     #ssl {
     #  enabled = true
     #  keystore-path = "src/main/resources/certs/keystore.p12"
     #  keystore-password = "changeit"
     #  keystore-type = "PKCS12"  # Options: PKCS12, JKS
     #}
  }

  # Database Configuration - Slick with PostgreSQL + slick-pg (Neon DB)
  # Note: Code uses TurnstilePostgresProfile which extends slick-pg for JSONB support
  database {
    profile = "slick.jdbc.PostgresProfile$"

    db {
      driver = "org.postgresql.Driver"
      url = "jdbc:postgresql://ep-summer-art-afxgi6oa-pooler.c-2.us-west-2.aws.neon.tech/turnstile?sslmode=require"
      url = ${?DATABASE_URL}  # Override via environment variable
      user = "neondb_owner"
      user = ${?DATABASE_USER}
      password = "npg_bSLfg7cVErp9"
      password = ${?DATABASE_PASSWORD}

      # Connection pool settings (HikariCP)
      numThreads = 10
      maxConnections = 10
      minConnections = 1
      connectionTimeout = 5000
      keepAliveConnection = true

      # SSL/TLS properties for Neon DB
      properties {
        ssl = true
        sslmode = "require"
      }
    }
  }
}

pekko {
  loglevel = DEBUG

  http.server.preview.enable-http2 = on

  http {
    server {
      idle-timeout = 600s
    }
  }

  actor {
    provider = cluster

    allow-java-serialization = on

    serialization-bindings {
      "java.lang.String" = java
      "com.google.protobuf.Message" = proto
      "app.dragon.turnstile.actor.TurnstileSerializable" = jackson-json
    }
  }

  remote.artery {
    canonical {
      hostname = "127.0.0.1"
      port = 25520
    }
  }

  cluster {
    seed-nodes = [
      "pekko://turnstile@127.0.0.1:25520"
    ]

    sharding {
      number-of-shards = 100

      passivation {
        strategy = default-strategy
        default-strategy {
          active-entity-limit = 1000000
        }
      }
    }

    downing-provider-class = "org.apache.pekko.cluster.sbr.SplitBrainResolverProvider"
  }

  management {
    http {
      hostname = "127.0.0.1"
      port = 8558
    }

    cluster.bootstrap {
      contact-point-discovery {
        service-name = "turnstile"
        discovery-method = config
      }
      contact-point {
        required-contact-point-nr = 1
      }
    }
  }

  discovery {
    method = config
    config.services {
      turnstile {
        endpoints = [
          {
            host = "127.0.0.1"
            port = 8558
          }
        ]
      }
    }
  }
}
